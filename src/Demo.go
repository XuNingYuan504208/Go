package main

import (
	"fmt"
)

func goFunc(i int) {

	fmt.Println("i:", i)
}

func main1() {
	/*for i := 0; i < 1000; i++ {
		go goFunc(i) // go 关键字开启协程
	}
	time.Sleep(time.Second)*/
	//Var.declare()

}
